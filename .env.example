# OpenAI-compatible API key (required)
OPENAI_API_KEY=sk-...

# Optional: override the LLM model (default: gpt-4o)
# LEISECA_MODEL=gpt-4o

# Optional: override the output directory (default: /mnt/user-data/outputs)
# LEISECA_OUTPUT_DIR=./outputs
